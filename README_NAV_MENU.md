# Navigation Menu Management System

ระบบจัดการเมนูนำทางแบบ Dynamic โดยใช้ Database

## 📁 โครงสร้างไฟล์

```
green_theme/
├── admin/
│   └── nav_menu.php           # หน้าจัดการเมนู (CRUD)
├── config/
│   └── database.php           # ไฟล์ตั้งค่าฐานข้อมูล
├── database/
│   └── nav_menu.sql           # SQL Script สำหรับสร้างตาราง
├── includes/
│   └── nav_menu_loader.php    # ไฟล์โหลดเมนูจาก database
└── index.php                  # หน้าแรก (ใช้เมนูจาก database)
```

## 🚀 วิธีการติดตั้ง

### ขั้นตอนที่ 1: สร้าง Database

1. เปิด phpMyAdmin (`http://localhost/phpmyadmin`)
2. สร้าง Database ชื่อ `green_theme_db`
3. เลือก Database ที่สร้าง แล้วไปที่แท็บ "SQL"
4. คัดลอกโค้ดจากไฟล์ `database/nav_menu.sql` แล้ววางในช่อง SQL
5. กดปุ่ม "Go" เพื่อรันคำสั่ง

### ขั้นตอนที่ 2: ตั้งค่า Database Connection

1. เปิดไฟล์ `config/database.php`
2. แก้ไขข้อมูลการเชื่อมต่อฐานข้อมูล (ถ้าจำเป็น):

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_NAME', 'green_theme_db');
```

### ขั้นตอนที่ 3: ทดสอบระบบ

1. เปิดหน้าแรก: `http://localhost/green_theme/index.php`
2. เข้าหน้าจัดการเมนู: `http://localhost/green_theme/admin/nav_menu.php`

## 📊 โครงสร้างตาราง nav_menu

| Column | Type | Description |
|--------|------|-------------|
| id | INT | Primary Key (Auto Increment) |
| parent_id | INT | NULL = Parent Menu, มีค่า = Submenu |
| menu_name | VARCHAR(100) | ชื่อเมนู (ภาษาไทย) |
| menu_name_en | VARCHAR(100) | ชื่อเมนู (ภาษาอังกฤษ) |
| menu_url | VARCHAR(255) | URL/Link ของเมนู |
| menu_icon | VARCHAR(50) | Font Awesome icon class |
| menu_order | INT | ลำดับการแสดงผล |
| is_active | TINYINT(1) | 1 = แสดง, 0 = ซ่อน |
| target | VARCHAR(20) | _self, _blank, _parent, _top |
| description | TEXT | คำอธิบายเพิ่มเติม |
| created_at | TIMESTAMP | วันที่สร้าง |
| updated_at | TIMESTAMP | วันที่แก้ไขล่าสุด |

## 🎯 วิธีการใช้งาน

### การเพิ่มเมนูใหม่

1. เข้าหน้าจัดการเมนู (`admin/nav_menu.php`)
2. กรอกข้อมูลในฟอร์มด้านขวา:
   - **Parent Menu**: เลือกว่าเป็น Parent หรือ Submenu
   - **ชื่อเมนู (TH)**: ชื่อเมนูภาษาไทย (บังคับ)
   - **ชื่อเมนู (EN)**: ชื่อเมนูภาษาอังกฤษ
   - **URL**: ลิงก์ของเมนู (ใส่ # ถ้าเป็น dropdown)
   - **Icon**: รหัส Font Awesome (เช่น `fas fa-home`)
   - **ลำดับ**: เรียงลำดับการแสดงผล (น้อยไปมาก)
   - **Target**: เปิดหน้าใหม่หรือหน้าเดิม
   - **เปิดใช้งาน**: เช็คถ้าต้องการให้แสดง
3. กดปุ่ม "เพิ่มเมนู"

### การแก้ไขเมนู

1. คลิกไอคอน "✏️" (Edit) ที่เมนูที่ต้องการแก้ไข
2. ข้อมูลจะแสดงในฟอร์มด้านขวา
3. แก้ไขข้อมูลที่ต้องการ
4. กดปุ่ม "บันทึกการแก้ไข"

### การลบเมนู

1. คลิกไอคอน "🗑️" (Delete) ที่เมนูที่ต้องการลบ
2. ยืนยันการลบ
3. **หมายเหตุ**: การลบ Parent Menu จะลบ Submenu ทั้งหมดด้วย

### การเปิด/ปิดเมนู

1. คลิกที่สถานะ "✅ เปิด" หรือ "❌ ปิด" เพื่อเปลี่ยนสถานะ
2. เมนูที่ปิดจะไม่แสดงในหน้าเว็บ

## 🔧 การปรับแต่ง

### เปลี่ยนสี Theme

แก้ไขในไฟล์ `admin/nav_menu.php`:

```php
// จาก teal-600 เป็นสีอื่น
<button class="bg-teal-600 hover:bg-teal-700">
```

### เพิ่มฟิลด์ใหม่ในตาราง

1. แก้ไข SQL ในไฟล์ `database/nav_menu.sql`
2. เพิ่มฟิลด์ในฟอร์ม (`admin/nav_menu.php`)
3. แก้ไข query INSERT/UPDATE
4. อัพเดต `includes/nav_menu_loader.php` ให้แสดงฟิลด์ใหม่

## 📝 ตัวอย่าง Menu Structure

```
หน้าแรก
บริการออนไลน์ ▼
  ├── แจ้งซ่อมคอมพิวเตอร์
  ├── ขอใช้อินเทอร์เน็ต
  └── ขอพื้นที่เก็บข้อมูล
คู่มือการใช้งาน ▼
  ├── Email เทศบาล
  ├── NAS
  └── Internet
ติดต่อเรา
```

## ❓ FAQ

**Q: เมนูไม่แสดงในหน้าเว็บ?**
A: ตรวจสอบว่า:
- Database connection ถูกต้อง
- เมนูมีสถานะ `is_active = 1`
- ไฟล์ `includes/nav_menu_loader.php` ถูก include ใน index.php

**Q: ต้องการเพิ่ม icon แบบอื่น?**
A: ใช้ Font Awesome icons จาก https://fontawesome.com/icons
คัดลอกรหัส class เช่น `fas fa-user`, `fas fa-heart`

**Q: ต้องการให้เมนูเปิดหน้าใหม่?**
A: เลือก Target = `_blank` ในฟอร์ม

**Q: จะเปลี่ยนลำดับเมนูอย่างไร?**
A: แก้ไขค่า "ลำดับ" ของเมนู ระบบจะเรียงตามลำดับจากน้อยไปมาก

## 🎓 เอกสารเพิ่มเติม

- [Font Awesome Icons](https://fontawesome.com/icons)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [PHP MySQLi Tutorial](https://www.w3schools.com/php/php_mysql_intro.asp)

---

**สร้างโดย**: Green Theme Development Team
**อัพเดทล่าสุด**: <?php echo date('d/m/Y'); ?>
