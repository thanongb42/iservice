# Quick Start - ติดตั้งระบบอย่างรวดเร็ว

## ⚡ ขั้นตอนการติดตั้ง (5 นาที)

### ขั้นตอนที่ 1: สร้างฐานข้อมูล

เปิด **phpMyAdmin** แล้วรันคำสั่งนี้:

```sql
CREATE DATABASE green_theme CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

### ขั้นตอนที่ 2: รัน SQL Files ตามลำดับ

**⚠️ สำคัญมาก: ต้องรันตามลำดับนี้เท่านั้น**

#### 1. รันไฟล์ `prefixes.sql`
```
1. เลือกฐานข้อมูล green_theme
2. ไปที่แท็บ "Import"
3. เลือกไฟล์: database/prefixes.sql
4. กด "Go"
```

**ผลลัพธ์:**
- ✅ สร้างตาราง `prefixes`
- ✅ เพิ่มข้อมูลคำนำหน้า 60+ รายการ

---

#### 2. รันไฟล์ `departments.sql`
```
1. Import: database/departments.sql
2. กด "Go"
```

**ผลลัพธ์:**
- ✅ สร้างตาราง `departments`
- ✅ เพิ่มข้อมูลหน่วยงานตัวอย่าง 30+ หน่วยงาน

---

#### 3. รันไฟล์ `users.sql`
```
1. Import: database/users.sql
2. กด "Go"
```

**ผลลัพธ์:**
- ✅ สร้างตาราง `users` (พร้อม prefix_id และ department_id)
- ✅ เพิ่มผู้ใช้ตัวอย่าง 6 คน (รวม admin)
- ✅ สร้าง View: `v_users_full`
- ✅ สร้าง Stored Procedures
- ✅ สร้าง Triggers

---

#### 4. (Optional) รันไฟล์ `add_username_field.sql`
**หมายเหตุ:** ไฟล์นี้ไม่จำเป็นต้องรัน เพราะ `users.sql` มีฟิลด์ username อยู่แล้ว
(ใช้เฉพาะกรณีที่ต้องเพิ่ม username ให้กับตาราง users เก่าที่มีอยู่แล้ว)

---

### ขั้นตอนที่ 3: แก้ไข Config

แก้ไขไฟล์ `config/database.php`:

```php
<?php
$host = 'localhost';
$dbname = 'green_theme';  // ใช้ชื่อเดียวกับที่สร้างไว้
$username = 'root';        // username MySQL ของคุณ
$password = '';            // password MySQL ของคุณ (XAMPP ค่าเริ่มต้นคือ ว่าง)

$conn = new mysqli($host, $username, $password, $dbname);
$conn->set_charset("utf8mb4");

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>
```

---

### ขั้นตอนที่ 4: ทดสอบระบบ

#### 1. เปิดเบราว์เซอร์
```
http://localhost/green_theme/
```

#### 2. ทดสอบ Login
```
http://localhost/green_theme/login.php
```

**ใช้บัญชี Admin:**
- Username: `admin`
- Password: `admin123`

#### 3. ทดสอบ Register
```
http://localhost/green_theme/register.php
```

---

## 👥 บัญชีผู้ใช้ที่พร้อมใช้งาน

| Username | Password | Role | ชื่อเต็ม | Email |
|----------|----------|------|----------|-------|
| **admin** | admin123 | admin | นาย ผู้ดูแลระบบ เทศบาล | admin@rangsit.go.th |
| somchai | admin123 | staff | นาย สมชาย ใจดี | somchai@rangsit.go.th |
| somsri | admin123 | user | นาง สมศรี รักดี | somsri@rangsit.go.th |
| suree | admin123 | user | นางสาว สุรีย์ สว่างใจ | suree@rangsit.go.th |
| military_user | admin123 | user | ว่าที่ร้อยตรี ธนพล มั่นคง | thanaphon@rangsit.go.th |
| doctor_user | admin123 | user | ดร. วิชัย ปัญญา | wichai@rangsit.go.th |

---

## 🎯 หน้าสำคัญที่ควรทดสอบ

### หน้า Public (ไม่ต้อง Login)
- ✅ `/index.php` - หน้าแรก
- ✅ `/login.php` - เข้าสู่ระบบ
- ✅ `/register.php` - สมัครสมาชิก

### หน้า Admin (Login เป็น admin)
- ✅ `/admin/index.php` - Dashboard
- ✅ `/admin/departments.php` - จัดการหน่วยงาน
- ✅ `/admin/service_requests.php` - จัดการคำขอบริการ

---

## ❌ Troubleshooting

### ปัญหา 1: Error 1146 - Table doesn't exist
**สาเหตุ:** รัน SQL ไม่ตามลำดับ

**วิธีแก้:**
1. Drop database แล้วสร้างใหม่:
```sql
DROP DATABASE IF EXISTS green_theme;
CREATE DATABASE green_theme CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```
2. รัน SQL ตามลำดับใหม่: prefixes.sql → departments.sql → users.sql

---

### ปัญหา 2: Error 1452 - Foreign key constraint fails
**สาเหตุ:** ตาราง parent ยังไม่มี

**วิธีแก้:**
- ต้องรัน `prefixes.sql` และ `departments.sql` ก่อน `users.sql`

---

### ปัญหา 3: Cannot connect to database
**สาเหตุ:** Config ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ `config/database.php`
2. ตรวจสอบ MySQL service ทำงานหรือไม่
3. ตรวจสอบ username/password

---

### ปัญหา 4: Login ไม่ได้
**สาเหตุ:** ข้อมูล users ไม่ถูกต้อง

**วิธีแก้:**
ตรวจสอบว่ามีข้อมูล admin หรือไม่:
```sql
SELECT * FROM users WHERE username = 'admin';
```

ถ้าไม่มี ให้รัน:
```sql
INSERT INTO users (prefix_id, username, first_name, last_name, email, password, role, status) VALUES
(1, 'admin', 'ผู้ดูแลระบบ', 'เทศบาล', 'admin@rangsit.go.th',
'$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi',
'admin', 'active');
```

---

## 📊 ตรวจสอบว่าติดตั้งสำเร็จ

รันคำสั่งนี้ใน phpMyAdmin:

```sql
-- ตรวจสอบจำนวนคำนำหน้า
SELECT COUNT(*) as total_prefixes FROM prefixes;
-- ควรได้: 60+

-- ตรวจสอบจำนวนหน่วยงาน
SELECT COUNT(*) as total_departments FROM departments;
-- ควรได้: 30+

-- ตรวจสอบจำนวนผู้ใช้
SELECT COUNT(*) as total_users FROM users;
-- ควรได้: 6

-- ตรวจสอบ admin
SELECT username, email, role FROM users WHERE role = 'admin';
-- ควรได้: admin | admin@rangsit.go.th | admin

-- ทดสอบ View
SELECT * FROM v_users_full LIMIT 5;
-- ควรแสดงข้อมูลผู้ใช้พร้อมคำนำหน้าและหน่วยงาน
```

---

## ✅ Checklist การติดตั้ง

- [ ] สร้างฐานข้อมูล `green_theme`
- [ ] รัน `prefixes.sql` สำเร็จ
- [ ] รัน `departments.sql` สำเร็จ
- [ ] รัน `users.sql` สำเร็จ
- [ ] แก้ไข `config/database.php`
- [ ] เปิด `http://localhost/green_theme/` ได้
- [ ] Login ด้วย admin/admin123 ได้
- [ ] Register สมาชิกใหม่ได้
- [ ] เข้า Admin panel ได้

---

## 🔄 การรีเซ็ตระบบ

หากต้องการเริ่มใหม่ทั้งหมด:

```sql
-- ลบฐานข้อมูลเดิม
DROP DATABASE IF EXISTS green_theme;

-- สร้างใหม่
CREATE DATABASE green_theme CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- รัน SQL ใหม่ตามลำดับ
-- 1. prefixes.sql
-- 2. departments.sql
-- 3. users.sql
```

---

## 📞 ต้องการความช่วยเหลือ?

หากติดปัญหา:
1. อ่าน [INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md) แบบละเอียด
2. ตรวจสอบ error log ใน phpMyAdmin
3. ตรวจสอบ PHP error log

---

**เวอร์ชัน:** 1.0.0
**วันที่อัปเดต:** 30 ธันวาคม 2568
**เวลาติดตั้ง:** ~5 นาที
