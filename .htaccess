<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /green_theme/

    # Redirect to public folder if it exists
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
